{
  "name": "@visor/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node scripts/build-extension.mjs",
    "package": "node scripts/build-extension.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@visor/compiler": "0.1.0",
    "@visor/extractor": "0.1.0",
    "@visor/protocol": "0.1.0",
    "@visor/schemas": "0.1.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "esbuild": "^0.25.5",
    "typescript": "^5.4.5"
  }
}
